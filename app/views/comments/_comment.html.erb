<div id="<%= dom_id comment %>">
  <li class="list-group-item">
    <small class="text-body-secondary">
      <%= comment.created_at.strftime("%B %d %Y, %l:%M%P") %> - <b><%= comment.body %></b>
    </small>
  </li>
</div>
